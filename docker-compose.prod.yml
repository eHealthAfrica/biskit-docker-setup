version: '3.8'

services:

  #-----------------------------------#
  #       CKAN                        #
  #-----------------------------------#
  ckan:
    image: biskit-ckan
    build:
      context: ./ckan
      dockerfile: Dockerfile.alpine
      args:
        GITHUB_TOKEN: ${GITHUB_TOKEN}
    volumes:
      - ckan_storage:/var/lib/ckan
